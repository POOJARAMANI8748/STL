#include <bits/stdc++.h>
using namespace std;
int main()
{
    array<int, 100> arr;
    arr.fill(10);   // will initialize entire array with 10
    arr.at(1) = 20; // We can give value at perticuler index
}